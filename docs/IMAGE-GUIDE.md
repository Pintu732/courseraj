# Images

V3 uses public image URLs to stay compatible with your current no-paid-storage setup.

For each course, paste an HTTPS thumbnail URL into Admin → Courses.
For the logo, paste an HTTPS logo URL into Admin → Website Settings, or keep `assets/icons/logo.svg`.

Do not paste Windows paths such as C:\Users\....
